version: '3.9'
services:
  gnome:
    container_name: Gnome-Explorer
    environment:
      - DISPLAY=10.100.1.5:0.1
    image: net-skoona-gnome-image:1.0.2
    networks:
      vlan20:
        ipv4_address: 10.100.20.9
    restart: always
    tty: true
    volumes:
      - type: volume
        source: cDevelopment
        target: /Projects
volumes:
  cDevelopment:
    external: true
   
networks:
  vlan20:
    name: iot-dev-network
    driver: macvlan
    driver_opts:
      parent: ens18.20
    ipam:
      config:
        - subnet: "10.100.20.0/24"
          gateway: "10.100.20.1"
